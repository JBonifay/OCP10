<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- =============================================================== -->
  <!-- Informations du projet -->
  <!-- =============================================================== -->
  <!-- ===== Informations Maven ===== -->
  <groupId>com.openclassrooms</groupId>
  <artifactId>bibliotheque</artifactId>
  <version>1.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <!-- ===== Informations générales ===== -->
  <name>Bibliotheque OC P7</name>
  <description>Projet 7 du parcours Openclassrooms DA Java</description>
  <url />
  <developers>
    <developer>
      <id>JBonifay</id>
      <name>Joffrey Bonifay</name>
      <email>joffreybonifay83@gmail.Com</email>
      <roles>
        <role>developer</role>
      </roles>
    </developer>
  </developers>

  <!-- ===== Spring boot parent ===== -->
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>2.2.1.RELEASE</version>
    <relativePath />
  </parent>

  <!-- ===== Modules ===== -->
  <modules>
    <module>batch</module>
    <module>config-server</module>
    <module>ouvrage</module>
    <module>reservation</module>
    <module>utilisateur</module>
    <module>web</module>
  </modules>

  <!-- ===== Build config ===== -->
  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <version>${spring-boot-maven-plugin}</version>
        <executions>
          <execution>
            <goals>
              <goal>repackage</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <!-- ===== Dependencies versions ===== -->
  <properties>
    <spring.cloud.starter.config>2.1.4.RELEASE</spring.cloud.starter.config>
    <spring-boot-starter-data-jpa>2.2.1.RELEASE</spring-boot-starter-data-jpa>
    <spring-boot-starter-thymeleaf>2.2.1.RELEASE</spring-boot-starter-thymeleaf>
    <spring-boot-starter-web>2.2.1.RELEASE</spring-boot-starter-web>
    <spring-boot-devtools>2.2.1.RELEASE</spring-boot-devtools>
    <spring-boot-starter-data-jpa>2.2.1.RELEASE</spring-boot-starter-data-jpa>
    <spring-boot-starter-batch>2.2.1.RELEASE</spring-boot-starter-batch>
    <spring-cloud-starter-openfeign>2.1.3.RELEASE</spring-cloud-starter-openfeign>
    <spring-boot-configuration-processor>2.1.8.RELEASE</spring-boot-configuration-processor>
    <spring-boot-maven-plugin>2.2.1.RELEASE</spring-boot-maven-plugin>
    <lombok>1.18.10</lombok>
  </properties>


  <!-- Add typical dependencies for a web application -->
  <dependencyManagement>
    <dependencies>
      <dependency>
        <artifactId>spring-cloud-starter-config</artifactId>
        <groupId>org.springframework.cloud</groupId>
        <version>${spring.cloud.starter.config}</version>
      </dependency>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-data-jpa</artifactId>
        <version>${spring-boot-starter-data-jpa}</version>
      </dependency>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-thymeleaf</artifactId>
        <version>${spring-boot-starter-thymeleaf}</version>
      </dependency>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
        <version>${spring-boot-starter-web}</version>
      </dependency>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-data-jpa</artifactId>
        <version>${spring-boot-starter-data-jpa}</version>
      </dependency>
      <dependency>
        <artifactId>spring-cloud-starter-openfeign</artifactId>
        <groupId>org.springframework.cloud</groupId>
        <version>${spring-cloud-starter-openfeign}</version>
      </dependency>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-configuration-processor</artifactId>
        <version>${spring-boot-configuration-processor}</version>
      </dependency>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-batch</artifactId>
        <version>${spring-boot-starter-batch}</version>
      </dependency>

      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-devtools</artifactId>
        <version>${spring-boot-devtools}</version>
        <scope>runtime</scope>
        <optional>true</optional>
      </dependency>
      <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <optional>true</optional>
        <version>${lombok}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

</project>